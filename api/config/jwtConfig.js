
//ATTENTION 
//this would be stored as an environmental variable in a file 
//that’s not checked in to Github, but to show how this works, 
//I’ve set it in here.

module.exports = {
    secret: 'jwt-secret',
  };